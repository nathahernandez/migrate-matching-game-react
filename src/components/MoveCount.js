import React from "react";

function MoveCount(props){
    
    return (
        <div id="moveCount">
                <p>Move Count: {props.cont}</p>
        </div>
    );
}

export default MoveCount;